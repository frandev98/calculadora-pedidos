package com.francisco.calculadorapedidos.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA PSICOLÓGICA FUXION ---

// 1. Fondos (Calma y Limpieza)
val BackgroundWhite = Color(0xFFF9FAFB) // No es blanco puro, es un gris muy sutil para no cansar la vista
val SurfaceWhite = Color(0xFFFFFFFF)    // Para las tarjetas

// 2. Acción y Éxito (Dopamina Visual)
val FuxionGreen = Color(0xFF4CAF50)     // El color de "Meta Lograda"
val FuxionBlue = Color(0xFF2196F3)      // Color de "Acción" (Botones, Links)
val ProgressOrange = Color(0xFFFF9800)  // Color de "Estás en camino" (Barra de carga)

// 3. Textos (Legibilidad sin fatiga)
val TextPrimary = Color(0xFF263238)     // Gris oscuro (mejor que negro puro)
val TextSecondary = Color(0xFF78909C)   // Gris azulado para detalles
val TextSuccess = Color(0xFF2E7D32)     // Verde oscuro para textos de puntos ganados

// 4. Estados
val ErrorRed = Color(0xFFE57373)        // Un rojo suave, no alarmante
val BadgeBackground = Color(0xFFE3F2FD) // Fondo azulito para etiquetas